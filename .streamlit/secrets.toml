# LOCAL DEVELOPMENT ONLY — never commit this file
# On Streamlit Cloud, configure via App Settings > Secrets in the dashboard
#
# Root-level key: automatically promoted to os.environ["ANTHROPIC_API_KEY"]
# ANTHROPIC_API_KEY loaded from .env via python-dotenv
