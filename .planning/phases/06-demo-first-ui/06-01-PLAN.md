---
phase: 06-demo-first-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - tests/test_ui.py
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04]
must_haves:
  truths:
    - "A visitor landing on the app sees a hero section explaining what Autopitch does before any interactive element"
    - "Clicking 'Try the Demo' generates a deck from bundled Apple data and presents a download button"
    - "The download button does not disappear after clicking it (session state persistence)"
    - "Elapsed time and slide count are displayed next to the download button after generation"
  artifacts:
    - path: "app.py"
      provides: "Complete hero section + demo generation flow with session state"
      contains: "st.session_state"
    - path: "tests/test_ui.py"
      provides: "Automated verification of DEMO-01 through DEMO-04"
      contains: "test_hero_section"
  key_links:
    - from: "app.py"
      to: "autopitch/pipeline.py"
      via: "@st.cache_data wrapped _run_demo_pipeline()"
      pattern: "cache_data.*_run_demo_pipeline"
    - from: "app.py"
      to: "demo/apple_financials.xlsx"
      via: "Path reference in cached demo function"
      pattern: "demo/apple_financials"
    - from: "app.py"
      to: "st.session_state"
      via: "demo_pptx, demo_elapsed, demo_slides keys"
      pattern: "session_state.*demo_pptx"
---

<objective>
Rewrite app.py from its current minimal upload-only form into a portfolio-ready page with a hero landing section and one-click demo generation. The demo path uses session state to persist PPTX bytes across reruns and displays generation stats.

Purpose: A portfolio visitor arriving at the live URL should immediately understand what Autopitch does and be able to try it with zero friction — one button click, no data needed.
Output: Rewritten app.py with hero + demo sections; tests/test_ui.py covering DEMO-01 through DEMO-04.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-first-ui/06-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs from existing codebase -->

From autopitch/pipeline.py:
```python
def run_pipeline(source: Union[str, Path, BinaryIO]) -> bytes:
    """Run the full Autopitch pipeline. Returns PPTX file as bytes."""
```

From autopitch/models.py:
```python
class ValidationError(Exception):
    """Raised when input data fails validation."""
```

From app.py (current — will be fully rewritten):
```python
# Current: minimal upload-only UI, 38 lines
# Page config: st.set_page_config(page_title="Autopitch", page_icon="...", layout="centered")
# Imports: run_pipeline, ValidationError, load_dotenv
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UI test scaffold and write failing tests for hero + demo flow</name>
  <files>tests/test_ui.py</files>
  <behavior>
    - test_hero_section_content: app.py contains st.title or st.header with "Autopitch" and descriptive text before any st.button or st.file_uploader call
    - test_demo_pipeline_cached: a function named _run_demo_pipeline exists in app.py and is decorated with @st.cache_data
    - test_demo_bytes_in_session_state: app.py references st.session_state["demo_pptx"] for storing pipeline output
    - test_demo_stats_in_session_state: app.py stores demo_elapsed and demo_slides in st.session_state
    - test_session_state_initialization: app.py initializes demo_pptx, demo_elapsed, demo_slides keys before first use
    - test_download_button_on_click_ignore: demo download button uses on_click="ignore" parameter
  </behavior>
  <action>
    Create tests/test_ui.py with structural tests that inspect app.py source code for required patterns. Use pytest with simple file reading and regex/string matching — no Streamlit AppTest needed for structural checks.

    Test approach:
    - Read app.py source as text
    - Verify hero content appears before interactive widgets (check line numbers)
    - Verify _run_demo_pipeline function exists with @st.cache_data decorator
    - Verify session_state keys are initialized and used
    - Verify on_click="ignore" on download buttons

    Also add a unit test for _build_template_xlsx (import from app module) that checks the returned bytes start with PK (valid ZIP/XLSX header) — this covers UPLD-03 for Plan 02 but the function will be created in this plan as part of the full app.py rewrite.

    Run tests — they MUST fail (RED phase). Commit with prefix test(06-01).
  </action>
  <verify>
    <automated>cd C:/Users/Chris/Desktop/Autopitch && python -m pytest tests/test_ui.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>tests/test_ui.py exists with 6+ failing tests covering DEMO-01 through DEMO-04 patterns</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app.py with hero section and demo generation flow</name>
  <files>app.py</files>
  <action>
    Rewrite app.py completely. The new file structure (top to bottom):

    1. **Imports and page config:**
       - import streamlit, time, pathlib.Path, io.BytesIO, openpyxl
       - from autopitch.pipeline import run_pipeline
       - from autopitch.models import ValidationError
       - st.set_page_config(page_title="Autopitch", page_icon="...", layout="wide")
       - NOTE: Remove load_dotenv() — not needed on Streamlit Cloud (secrets.toml handles it); keep import only if .env fallback desired for local CLI use. Actually keep it for local dev compatibility.

    2. **Session state initialization:**
       - Initialize keys: demo_pptx, demo_elapsed, demo_slides (all default None)
       - Initialize keys: upload_pptx (for Plan 02, default None)
       - Use the guard pattern: `if key not in st.session_state: st.session_state[key] = None`

    3. **Cached demo pipeline function:**
       ```python
       @st.cache_data(show_spinner=False)
       def _run_demo_pipeline() -> bytes:
           return run_pipeline(Path("demo/apple_financials.xlsx"))
       ```

    4. **Template builder function (for Plan 02 but define here):**
       ```python
       def _build_template_xlsx() -> bytes:
           # Create openpyxl workbook with P&L, Balance Sheet, Cash Flow sheets
           # with correct headers and row labels per TEMPLATE_FORMAT.md
           # Return buf.getvalue() (bytes, not BytesIO)
       ```

    5. **Hero section (DEMO-01):**
       - st.title("Autopitch")
       - st.markdown with 2-3 sentences: what it does, who it's for, what output looks like
       - Use st.columns for a clean layout if desired

    6. **Demo section (DEMO-02, DEMO-03, DEMO-04):**
       - st.subheader or st.header: "Try the Demo"
       - st.markdown: brief explanation ("See it in action with real Apple FY2020-2024 financials")
       - st.button("Try the Demo") — on click: time the pipeline call, store bytes + elapsed + slides (11) in session state
       - Use st.spinner during generation
       - OUTSIDE the if-button block: conditionally render download button + stats from session_state
       - Download button: on_click="ignore", correct MIME type, file_name="autopitch_apple_demo.pptx"
       - Stats: st.caption with elapsed time and slide count in columns layout

    7. **st.divider() — separator before upload section (placeholder for Plan 02)**

    8. **Upload section (minimal placeholder — Plan 02 will expand):**
       - Keep the existing upload functionality working but restructured
       - st.subheader("Upload Your Own Data")
       - st.file_uploader, generate button, download button with on_click="ignore"
       - Store upload_pptx in session state so download persists
       - Wrap in try/except ValidationError

    9. **st.divider() — separator before skills section (placeholder for Plan 02)**

    Key patterns from research (MUST follow):
    - on_click="ignore" on ALL download buttons (prevents rerun)
    - Session state for ALL generated bytes (demo and upload)
    - Download buttons rendered based on session_state, NOT inside if st.button() blocks
    - @st.cache_data only on demo pipeline (NOT upload)
    - buf.seek(0) then buf.getvalue() for BytesIO patterns

    Commit with prefix feat(06-01).
  </action>
  <verify>
    <automated>cd C:/Users/Chris/Desktop/Autopitch && python -m pytest tests/test_ui.py -x -q && python -m pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests/test_ui.py tests pass (GREEN). Full test suite still passes. app.py has hero section, demo generation with session state, cached pipeline, download button with on_click="ignore", and generation stats display.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ui.py -q` — all DEMO-01 through DEMO-04 tests green
- `python -m pytest -q` — full suite still passes (no regressions)
- `python -c "import app"` — app.py imports without error (syntax valid)
</verification>

<success_criteria>
- Hero section with Autopitch description appears before any interactive widget
- "Try the Demo" button triggers cached pipeline with Apple demo data
- PPTX bytes persist in session_state across reruns
- Download button uses on_click="ignore" and does not disappear
- Generation stats (elapsed time, slide count) displayed after demo generation
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-first-ui/06-01-SUMMARY.md`
</output>
