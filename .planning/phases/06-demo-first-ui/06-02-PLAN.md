---
phase: 06-demo-first-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app.py
  - tests/test_ui.py
autonomous: true
requirements: [UPLD-01, UPLD-02, UPLD-03, SKIL-01]
must_haves:
  truths:
    - "A visitor can upload their own Excel file using clearly labeled instructions"
    - "An expandable format guide shows required sheets and columns"
    - "A downloadable Excel template is available with correct structure"
    - "A tech stack section shows 5-6 technologies with one-sentence rationale per tool"
  artifacts:
    - path: "app.py"
      provides: "Upload section with format guide, template download, and tech stack showcase"
      contains: "st.expander"
    - path: "tests/test_ui.py"
      provides: "Tests for UPLD-01 through UPLD-03 and SKIL-01"
      contains: "test_upload_section"
  key_links:
    - from: "app.py"
      to: "openpyxl.Workbook"
      via: "_build_template_xlsx() function"
      pattern: "_build_template_xlsx"
    - from: "app.py"
      to: "autopitch/pipeline.py"
      via: "run_pipeline(uploaded) for custom upload path"
      pattern: "run_pipeline.*uploaded"
---

<objective>
Add the upload section (instructions, format guide accordion, template download) and tech stack showcase to app.py. This completes the four-section portfolio page structure.

Purpose: Visitors who want to try their own data get clear guidance and a downloadable template. Recruiters see the technical stack and architectural thinking behind the project.
Output: Updated app.py with all four sections complete; tests/test_ui.py extended with UPLD and SKIL tests.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-first-ui/06-RESEARCH.md
@.planning/phases/06-demo-first-ui/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — functions already in app.py -->

From app.py (after Plan 01):
```python
# Already exists:
@st.cache_data(show_spinner=False)
def _run_demo_pipeline() -> bytes: ...

def _build_template_xlsx() -> bytes: ...
# ^ Returns bytes for a valid XLSX with P&L, Balance Sheet, Cash Flow sheets

# Session state keys already initialized:
# demo_pptx, demo_elapsed, demo_slides, upload_pptx
```

From TEMPLATE_FORMAT.md (source of truth for format guide content):
```
Sheets: P&L, Balance Sheet, Cash Flow (exact names, case-sensitive)
Column A = row labels, columns B+ = fiscal years (FY2022, FY2023, ...)
P&L rows: Revenue, COGS, Operating Income, Depreciation & Amortization, Net Income
Balance Sheet rows: Current Assets, Current Liabilities, Total Assets, Total Debt, Total Shareholders Equity
Cash Flow rows: Operating Cash Flow, Capital Expenditures
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for upload section and skills showcase</name>
  <files>tests/test_ui.py</files>
  <behavior>
    - test_upload_section_exists: app.py contains st.file_uploader with type=["xlsx"] and instructional text
    - test_format_guide_expander: app.py contains st.expander with format guide content (sheet names, required rows)
    - test_template_xlsx_bytes: _build_template_xlsx() returns bytes starting with PK header and containing all three sheet names
    - test_template_xlsx_has_required_sheets: Parse returned bytes with openpyxl, verify P&L, Balance Sheet, Cash Flow sheets exist with correct row labels
    - test_tech_stack_section: app.py contains "How It's Built" or similar header and at least 5 technology names (Python, Streamlit, Claude, python-pptx, matplotlib)
    - test_upload_download_on_click_ignore: upload download button uses on_click="ignore"
  </behavior>
  <action>
    Extend tests/test_ui.py with new test functions for UPLD-01, UPLD-02, UPLD-03, and SKIL-01.

    For test_template_xlsx_bytes and test_template_xlsx_has_required_sheets: import _build_template_xlsx from app module and test the returned bytes directly. Use openpyxl.load_workbook(BytesIO(bytes)) to verify sheet structure.

    For structural tests: read app.py source and check for required patterns via string matching.

    Run tests — new tests MUST fail (RED). Commit with prefix test(06-02).
  </action>
  <verify>
    <automated>cd C:/Users/Chris/Desktop/Autopitch && python -m pytest tests/test_ui.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>6+ new failing tests in tests/test_ui.py covering UPLD-01 through UPLD-03 and SKIL-01</done>
</task>

<task type="auto">
  <name>Task 2: Add upload section with format guide and template, plus tech stack showcase</name>
  <files>app.py</files>
  <action>
    Extend app.py to fill in the upload and skills sections (Plan 01 left placeholder dividers).

    **Upload section (between demo and skills dividers):**

    1. st.subheader("Upload Your Own Data") — UPLD-01
    2. Brief instructions: "Upload your Excel workbook (.xlsx) with three sheets: P&L, Balance Sheet, and Cash Flow."
    3. st.expander("Format guide — required sheets and columns", expanded=False) — UPLD-02
       - Content from TEMPLATE_FORMAT.md: three sheets (exact names), column layout, required row labels for each sheet
       - Use st.markdown with a formatted table or bullet list
    4. Template download button — UPLD-03
       - Call _build_template_xlsx() (already defined in Plan 01)
       - st.download_button with label="Download Excel Template", correct XLSX MIME type, on_click="ignore"
    5. File uploader: st.file_uploader("Choose your Excel file", type=["xlsx"])
    6. Generate button (only shown when file uploaded)
    7. Results: store upload_pptx in session_state, render download button with on_click="ignore" outside button block
    8. Error handling: try/except ValidationError with st.error

    **Tech stack section (after upload section):**

    1. st.divider()
    2. st.subheader("How It's Built")
    3. Display 6 technologies in a clean layout using st.columns([1, 4]):
       - Python — Core language for data pipelines and financial analysis
       - Streamlit — Zero-frontend web UI for interactive portfolio demos
       - Claude (Anthropic) — LLM generates insight-first narrative from computed metrics
       - python-pptx — Programmatic PPTX assembly with Big 4 consulting aesthetics
       - matplotlib — Chart rendering embedded as high-DPI images in slides
       - openpyxl — Excel workbook parsing with data validation

    Commit with prefix feat(06-02).
  </action>
  <verify>
    <automated>cd C:/Users/Chris/Desktop/Autopitch && python -m pytest tests/test_ui.py -x -q && python -m pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests green including new UPLD and SKIL tests. app.py has complete four-section layout: hero, demo, upload (with format guide + template), and tech stack showcase. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ui.py -q` — all tests green including UPLD-01 through SKIL-01
- `python -m pytest -q` — full suite passes
- `python -c "from app import _build_template_xlsx; b = _build_template_xlsx(); assert b[:2] == b'PK'"` — template is valid XLSX
</verification>

<success_criteria>
- Upload section has clear instructions, file uploader accepting .xlsx files
- Expandable format guide shows required sheets, columns, and row labels
- Template download button serves valid XLSX with P&L, Balance Sheet, Cash Flow sheets
- Upload download button persists via session state with on_click="ignore"
- Tech stack section displays 5-6 technologies with one-line rationale each
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-first-ui/06-02-SUMMARY.md`
</output>
