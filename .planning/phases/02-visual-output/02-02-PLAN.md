---
phase: 02-visual-output
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - autopitch/charts.py
autonomous: true
requirements: [CHRT-01, CHRT-02, CHRT-03, CHRT-04, CHRT-05]

must_haves:
  truths:
    - "bar_chart(years, values, title, ylabel) returns a non-empty BytesIO containing a valid PNG at 150 DPI"
    - "line_chart(years, series, title, ylabel) handles 1-3 series and returns a non-empty BytesIO PNG"
    - "waterfall_chart(labels, values, title) returns a non-empty BytesIO PNG; positive values use TEAL_HEX, negative use NEGATIVE_HEX, first and last bars use NAVY_HEX"
    - "kpi_scorecard(metrics, title) returns a non-empty BytesIO PNG with text for each metric label and value"
    - "All chart functions use the Agg backend — no display window opens; plt.close(fig) called in every function"
  artifacts:
    - path: "autopitch/charts.py"
      provides: "bar_chart, line_chart, waterfall_chart, kpi_scorecard — all returning io.BytesIO"
      exports: ["bar_chart", "line_chart", "waterfall_chart", "kpi_scorecard"]
      min_lines: 80
  key_links:
    - from: "autopitch/charts.py"
      to: "autopitch/theme.py"
      via: "from autopitch.theme import NAVY_HEX, TEAL_HEX, LIGHT_GRAY_HEX, DARK_GRAY_HEX, POSITIVE_HEX, NEGATIVE_HEX, FONT_HEADING, FONT_BODY"
      pattern: "from autopitch\\.theme import"
    - from: "tests/test_charts.py"
      to: "autopitch/charts.py"
      via: "from autopitch.charts import bar_chart, line_chart, waterfall_chart, kpi_scorecard"
      pattern: "bar_chart|line_chart|waterfall_chart|kpi_scorecard"
---

<objective>
Implement autopitch/charts.py — the complete chart generation module that renders all four chart types as in-memory PNG images via matplotlib.

Purpose: Turns CHRT-01..05 test stubs GREEN. Provides the BytesIO images that deck.py will embed into slides in the next plan.
Output: autopitch/charts.py with bar_chart(), line_chart(), waterfall_chart(), kpi_scorecard() — all returning io.BytesIO.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-visual-output/02-RESEARCH.md
@autopitch/theme.py
@tests/test_charts.py

<interfaces>
<!-- theme.py constants that charts.py imports. Do not hardcode these values in charts.py. -->

From autopitch/theme.py (created in plan 02-01):
```python
NAVY_HEX       = "#002B49"
TEAL_HEX       = "#00838A"
LIGHT_GRAY_HEX = "#F2F2F2"
DARK_GRAY_HEX  = "#404040"
POSITIVE_HEX   = "#00838A"   # same as TEAL_HEX
NEGATIVE_HEX   = "#C0392B"
FONT_HEADING   = "Calibri"
FONT_BODY      = "Calibri"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement autopitch/charts.py</name>
  <files>autopitch/charts.py</files>
  <behavior>
    All functions must return a non-empty io.BytesIO containing a valid PNG:
    - bar_chart(years: list[str], values: list[float], title: str, ylabel: str) -> io.BytesIO
      - Bars colored NAVY_HEX; last bar TEAL_HEX
      - Y-axis formatted as "${x/1000:.0f}B" for values in millions (e.g. 394328 → "$394B")
      - CHRT-01 GREEN when test_bar_chart_returns_png passes
    - line_chart(years: list[str], series: dict[str, list[float]], title: str, ylabel: str) -> io.BytesIO
      - Up to 3 series colored [NAVY_HEX, TEAL_HEX, DARK_GRAY_HEX], markers='o', linewidth=2
      - Y-axis formatted as "{x:.1f}%" for margin data
      - CHRT-02 GREEN when test_line_chart_multi_series passes
    - waterfall_chart(labels: list[str], values: list[float], title: str) -> io.BytesIO
      - Stacked bar with invisible bottom (running cumulative baseline)
      - Positive values: POSITIVE_HEX (teal); negative values: NEGATIVE_HEX (red-orange)
      - First bar (index 0) and last bar (index -1) always NAVY_HEX regardless of sign
      - Last bar's running baseline = 0 (it is a final total drawn from zero)
      - CHRT-03 GREEN when test_waterfall_chart passes
    - kpi_scorecard(metrics: dict[str, str], title: str) -> io.BytesIO
      - ax.axis('off'); no axes, no spines
      - Grid: n_cols = min(5, len(items)), n_rows = ceil(len/n_cols)
      - Each cell: value text (Pt 28, NAVY_HEX, bold) above label text (Pt 9, DARK_GRAY_HEX)
      - CHRT-05 GREEN when test_kpi_scorecard passes
    - CHRT-04: All of the above use DPI=150 in savefig call → test_chart_dpi passes

    Private helpers (not exported):
    - _apply_consulting_style(ax, fig): remove top/right spines; gray grid; correct fonts
    - _to_bytesio(fig) -> io.BytesIO: savefig(buf, format='png', dpi=150, bbox_inches='tight'); buf.seek(0); plt.close(fig); return buf

    CRITICAL rules:
    - First two lines of file: `import matplotlib` then `matplotlib.use('Agg')` — before any pyplot import
    - Set rcParams at module level: font.family=sans-serif, font.sans-serif=['Calibri','Arial','DejaVu Sans']
    - ALWAYS call plt.close(fig) inside _to_bytesio after savefig — prevents memory leak
    - ALWAYS call buf.seek(0) before returning from _to_bytesio — add_picture reads from cursor position
    - Import numpy as np for waterfall cumulative sum: `running = np.zeros(n); for i in range(1,n): running[i] = running[i-1] + values[i-1]; running[-1] = 0`
  </behavior>
  <action>
    Implement autopitch/charts.py following the exact structure from 02-RESEARCH.md Pattern 2 and Pattern 4.

    The complete file structure:
    1. `import matplotlib` + `matplotlib.use('Agg')` (first two lines)
    2. `import matplotlib.pyplot as plt`
    3. `import matplotlib.ticker as mticker`
    4. `import numpy as np`
    5. `import io`
    6. `import matplotlib; plt.rcParams.update(...)` — set font cascade
    7. `from autopitch.theme import ...` — all hex and font constants
    8. Module-level constants: `DPI = 150`, `FIGSIZE_STANDARD = (10, 5.5)`
    9. `_apply_consulting_style(ax, fig)` private helper
    10. `_to_bytesio(fig)` private helper
    11. `bar_chart()` public function
    12. `line_chart()` public function
    13. `waterfall_chart()` public function
    14. `kpi_scorecard()` public function

    Waterfall math (exact):
    ```python
    n = len(values)
    running = np.zeros(n)
    for i in range(1, n):
        running[i] = running[i - 1] + values[i - 1]
    running[-1] = 0  # final total bar starts from zero
    colors = [POSITIVE_HEX if v >= 0 else NEGATIVE_HEX for v in values]
    colors[0] = NAVY_HEX
    colors[-1] = NAVY_HEX
    ax.bar(labels, values, bottom=running, color=colors, width=0.5, zorder=3)
    ```

    After implementing, run the test suite to confirm CHRT-01..05 turn GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_charts.py -v -q</automated>
  </verify>
  <done>
    All 5 chart tests pass GREEN:
    - test_bar_chart_returns_png
    - test_line_chart_multi_series
    - test_waterfall_chart
    - test_chart_dpi (PIL.Image.open verifies valid PNG and DPI=150)
    - test_kpi_scorecard
    Full suite: pytest tests/ -q shows all Phase 1 tests (22) still GREEN plus 5 new chart tests.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_charts.py -v` — 5 tests GREEN
- `pytest tests/ -q` — 22 + 5 = 27 tests pass, 0 failures
- `python -c "from autopitch.charts import bar_chart, line_chart, waterfall_chart, kpi_scorecard; print('OK')"` — prints OK
</verification>

<success_criteria>
- autopitch/charts.py exists with 4 public functions + 2 private helpers
- All 5 CHRT tests pass (test_bar_chart_returns_png, test_line_chart_multi_series, test_waterfall_chart, test_chart_dpi, test_kpi_scorecard)
- Agg backend set before pyplot import — no display window
- plt.close(fig) called in _to_bytesio — no memory leak
- buf.seek(0) called before return — no silent empty-image failures
- All Phase 1 tests (22) remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-output/02-02-SUMMARY.md` following the summary template.
</output>
