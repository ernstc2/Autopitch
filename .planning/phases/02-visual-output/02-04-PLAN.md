---
phase: 02-visual-output
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: []
autonomous: false
requirements: [CHRT-03, CHRT-04, DECK-01, DECK-02, DECK-03, DECK-04, DECK-05, DECK-06, DECK-07, DECK-08]

must_haves:
  truths:
    - "Opening output/apple_demo.pptx in PowerPoint or LibreOffice shows 11 slides with no missing/broken images"
    - "Slide 1 is a full-navy title slide with 'Apple Inc.' text clearly visible in white"
    - "All chart slides show embedded charts (not broken image placeholders)"
    - "Waterfall chart (slide 5) shows visually distinct positive (teal) and negative (red-orange) bars"
    - "Every slide shows a footer with company name and slide number"
    - "The navy header bar color matches Deloitte-style dark navy (not black, not blue)"
  artifacts:
    - path: "output/apple_demo.pptx"
      provides: "Generated Apple deck for visual review"
  key_links:
    - from: "output/apple_demo.pptx"
      to: "human reviewer"
      via: "Open in PowerPoint or LibreOffice Impress"
      pattern: "visual inspection"
---

<objective>
Human visual verification of the generated Apple deck. Confirm that output/apple_demo.pptx meets the Big 4 aesthetic requirements that cannot be machine-asserted: chart rendering fidelity, color accuracy, overall professional appearance.

Purpose: Automated tests verify structure and content. Only a human can confirm the deck looks boardroom-ready. This is the Phase 2 quality gate.
Output: Human approval (or a list of specific issues for automated fix in a gap-closure plan).
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-visual-output/02-VALIDATION.md
@.planning/phases/02-visual-output/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate output/apple_demo.pptx from demo data</name>
  <files></files>
  <action>
    Run the following script to ensure a fresh PPTX is generated from the Apple demo file before human review:

    ```python
    from autopitch.parser import parse_workbook
    from autopitch.validator import validate
    from autopitch.metrics import compute_metrics
    from autopitch.deck import build_deck

    data = parse_workbook("demo/apple_financials.xlsx")
    validate(data)
    metrics = compute_metrics(data)
    prs = build_deck(data, metrics)
    prs.save("output/apple_demo.pptx")
    print(f"Generated: output/apple_demo.pptx — {len(prs.slides)} slides")
    ```

    Also run full test suite to confirm all tests pass before human review:
    ```
    pytest tests/ -q
    ```
  </action>
  <verify>
    <automated>pytest tests/ -q && python -c "from autopitch.parser import parse_workbook; from autopitch.validator import validate; from autopitch.metrics import compute_metrics; from autopitch.deck import build_deck; data=parse_workbook('demo/apple_financials.xlsx'); validate(data); metrics=compute_metrics(data); prs=build_deck(data, metrics); prs.save('output/apple_demo.pptx'); print(f'{len(prs.slides)} slides OK')"</automated>
  </verify>
  <done>All tests pass; output/apple_demo.pptx is freshly generated and ready for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete 11-slide Apple Financial Performance deck generated from real FY2020-FY2024 data.
    File: output/apple_demo.pptx (open in PowerPoint or LibreOffice Impress).

    Expected slides:
    1. Title slide — full navy background, "Apple Inc.", "Financial Performance Review", period and date in white
    2. Executive Summary — navy header, placeholder metric bullets
    3. P&L | Revenue Trends — bar chart of Revenue FY2020-FY2024
    4. P&L | Margin Analysis — line chart of 3 margin metrics
    5. P&L | Earnings Bridge — waterfall chart (Revenue → Gross Profit → EBITDA → Net Income)
    6. Balance Sheet | Asset Composition — bar chart of Total Assets
    7. Balance Sheet | Working Capital — line chart of working capital metrics
    8. Balance Sheet | Leverage — bar chart of Total Debt
    9. Cash Flow | OCF vs FCF — bar chart of free cash flow
    10. Cash Flow | Trend — line chart of FCF trend
    11. KPI Scorecard — grid of 8 KPI metric values
  </what-built>
  <how-to-verify>
    1. Open output/apple_demo.pptx in PowerPoint or LibreOffice Impress
    2. Check slide count: should be 11 slides
    3. Slide 1: Full navy background, white text, "Apple Inc." visible, date present
    4. Slides 3-11: Every chart slide shows an embedded chart image (no broken/gray placeholder)
    5. Slide 5 (Earnings Bridge): Waterfall chart shows positive bars in teal, negative bars in red-orange
    6. All slides: Footer visible with "Apple Inc." and slide number (e.g. "1 / 11")
    7. Overall aesthetic: navy header bars, clean whitespace, professional appearance
    8. No slides have obvious layout errors (text overflowing off-slide, missing headers, etc.)
  </how-to-verify>
  <resume-signal>
    Type "approved" if the deck looks professional and all charts render correctly.
    OR describe any specific visual issues (e.g. "slide 5 waterfall bars are all same color", "footer text missing on slide 3") and the plan will be updated to fix them.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- [ ] 11 slides present
- [ ] All chart images render (no broken placeholders)
- [ ] Waterfall chart has positive/negative color distinction
- [ ] Footer with company name and slide numbers on every slide
- [ ] Big 4 navy/teal/gray aesthetic — professional appearance
</verification>

<success_criteria>
Human approves the deck as visually professional and all charts rendering correctly. Phase 2 is complete.
</success_criteria>

<output>
After human approval, create `.planning/phases/02-visual-output/02-04-SUMMARY.md` following the summary template.
Update REQUIREMENTS.md to mark CHRT-01..05 and DECK-01..08 as complete.
</output>
